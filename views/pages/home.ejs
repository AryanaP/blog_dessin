<!DOCTYPE html>
<html lang="en">
<head>
    <% include ../partials/head %>
</head>
<body class="container">

    <header>
        <% include ../partials/topbar %>
    </header>

    <main>
      <div class="row">
        <div class="col-sm-4">
          <div class="well">
             <% include ../partials/sidebar %>
          </div>
        </div>
        <div class="col-sm-8">
          <div class="jumbotron">
              <h1>Blog Dessin</h1>
              <p>Welcome to my blog</p>
            <!-- POSTER LE TEXTE -->
            <% if (albumlist == []) { %>
              <p> no pictures yet! </p>
            <% } else { %>
              <ul>
                <% for (var i=albumlist.length; i>0; i--) { %>
                  <p> Titre et album de la photo </p>
                  <li> <%= albumlist[i-1] %></li>
                <% } %>
              </ul>
            <% } %>
            <!-- Fin poster text -->

            <!-- poster photo test -->
            <div id="last-uploads">
            </div>

            <!-- fin poster photo -->
          </div>
        </div>
    </main>

    <footer>
        <% include ../partials/footer %>
    </footer>

</body>
</html>
